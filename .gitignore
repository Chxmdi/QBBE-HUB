# dependencies
/node_modules
.pnp
.pnp.*

# testing
/coverage
/playwright-report
/test-results

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — never commit secrets
.env
.env.*
!.env.example
# publishable (browser-shipped) values only; secrets stay in host env
!.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# supabase local
supabase/.temp
supabase/.branches

# playwright
/test-results
/playwright-report
/blob-report
.env.local

# Scratch worktrees created for parallel agent runs. Transient, and nested
# git worktrees must never be committed into the repository they check out.
.claude/worktrees/

# The governing specification, staged for the coverage audit only.
# NOT committed: this repository is public and the spec is an internal document.
docs/audit/.spec-source.md
