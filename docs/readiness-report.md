# Deployment readiness report

Verdict: **NOT READY**. Full implementation remains in progress; external
verification is blocked. No staging release candidate has been certified.

| Release item | Current evidence |
|---|---|
| Candidate | Epic 01 merged to `main` at `698c52f`. Epic 02 mostly on `main`: #26 landed through PR #72; #27, #28 and #76 landed together through PR #78, merged at `5c77f97` on 2026-09-21, after PR #74 and PR #77 were merged in the wrong order and left the #76 commits on a branch that had already been merged out. #31 is the only Epic 02 issue still open; epic #12 was reopened on 2026-09-21 because it had been closed ahead of it. #31 landed on `main` through PR #83, merged at `63c04ba` on 2026-09-22, and P0-FIL-01 followed through PR #84, merged at `eb0c74f` the same day, carrying all four of its rows plus the #79 membership fix; CI was green on that head (44 authenticated browser checks, 504 unit tests, 24 accessibility checks). P1-TSK-11 had been marked `implementing` for having no surface, and building that surface exposed two defects in the mechanism it was meant to expose. **Epic 02 now has no open issues.** Epic 03 has begun: P0-FIL-01 is on `main`; #35 itself stays open on P1-FIL-02, which is blocked on QBBE-hosted ClamAV. No certified release |
| Staging | No QBBE staging deployment provisioned in this session |
| Production | No publishing authorized by this plan; keep release disabled |
| Code checks | At `a1d3ab8`: lint 0 errors and 1 warning (the pre-existing `no-img-element` in `src/components/layout/qbbe-logo.tsx:31`), typecheck clean, `npm test` 504 passed across 54 files, production build passed. Previously at `f534497`: lint 0 errors and 1 warning (the pre-existing `no-img-element` in `src/components/layout/qbbe-logo.tsx:31`), typecheck clean, `npm test` 504 passed across 54 files, production build passed. Previously at `c748713`: lint 0 errors and 1 warning (the pre-existing `no-img-element` in `src/components/layout/qbbe-logo.tsx:31`), typecheck clean, `npm test` 504 passed across 54 files, production build passed |
| Database | Clean local reset applied migrations through `20260923210000`; `npm run test:db` 456 assertions across 19 files, exit 0; `npx supabase db advisors --local --type security --fail-on error` reports no issues. The 14 newest are `supabase/tests/events.sql`, covering #32: an `event_assignment.role` outside the seven areas, which plain `text` accepted until `20260923200000`; and a person from another organization being assigned to an event, which the write policy never checked because it asks who is assigning rather than who is assigned. Both were measured against the database before they were fixed, and the file fails against the unfixed schema. The 12 before them are `supabase/tests/invitation-organization.sql`, covering #90: `app.handle_new_user` chose the organization for a new account with `select id from organization limit 1` — an arbitrary row unrelated to the invitation — while reading the role out of the invitation, so the role arrived and the organization did not. Latent rather than live, because nothing in the application creates a second organization and production holds one; it would have become a cross-tenant defect, and an `admin` invitation from one organization would have conferred administration of another. The assertions run against a second organization, which is the only condition under which the old code was wrong, and the first of them fails against it. The 14 before them are `supabase/tests/document-links.sql`, covering the approved-source guard on external links (P0-FIL-01) including `javascript:`, `data:`, http, lookalike hosts and cross-organization approval, and naming both non-administrator roles in the two policy assertions. The 21 before them are `supabase/tests/work-planning.sql`: the milestone dependency cycle guard at two and three nodes, series occurrence uniqueness, checklist positioning, and the two identity probes that pin the membership misreport in #79 |
| Browsers | **2026-09-22: the standing bar is met for the first time.** Two consecutive clean full authenticated passes — 46 of 46 in 9.3 minutes and 46 of 46 in 9.7 minutes — on Node 22.23.2 with the server's outbound Supabase calls on a non-loopback address, one server process, no exits. Six passes ran against that process across about two hours; the four before these two each lost time to Windows Modern Standby on battery, and every failure in them was the machine sleeping rather than the product, confirmed against Kernel-Power events. A server exit is now reported as an exit rather than as test failures, by `tests/e2e/server-watchdog.ts`. Historic detail follows. 38 authenticated Chromium checks against real local Auth (`mfa`, `realtime-revocation`, `hello-hub`, `access-impact`, `my-work`, `identity-lifecycle`, `programs`, `projects`, `milestones`, `task-core`). **The standing bar of two consecutive clean full passes is met on no machine, local or CI, and this row does not claim it.** Three CI runs over product code that is identical apart from two Markdown files: `8c13f1a` 38 of 38 in 3.7 minutes; `5e093db` 36 of 38 (`access-impact:5`, `identity-lifecycle:195`); `c131ee5` 37 of 38 (`identity-lifecycle:195`). A documentation-only diff cannot break a browser test, so those three runs exonerate the code — but they also **withdraw an earlier claim in this row that CI had never reproduced the instability and was the stable instrument.** It has reproduced it twice. The precursor `destination stream closed early` appeared 14, 15 and 18 times across the three runs, including the one that passed everything, so it is background noise rather than a crash signal. Locally the best run was 37 of 38 and a second was 34 of 38, on a machine where `next start` crashed four times in roughly thirty-five minutes. Every one of the 38 has passed locally; local failures were re-run and passed, and `access-impact.spec.ts:87`, the only check to fail twice locally, then passed alone in 46.3s with no crash, having carried a different error each time. **One failure does not fit the environmental explanation and is not filed under it:** `identity-lifecycle.spec.ts:195` failed on CI twice with byte-identical symptoms — `net::ERR_ABORTED` at `http://127.0.0.1:3000/sign-in` from `page.goto` in the shared `signOut` helper at `tests/e2e/auth.ts:122` — having also failed locally once with `ERR_CONNECTION_REFUSED`. A bug that reproduces the same way is the test this report uses to separate a defect from an unstable machine, and this one now passes it. **Settled (#79, #88):** it was a race, but not the cookie clear on its own. A dialog that had just closed left `router.refresh()` in flight; clearing cookies turned that refresh into a real navigation back to the page, which superseded `signOut`'s `goto` and aborted it. CI run 35627688215 recorded it exactly (three `/my-work` RSC refreshes, `/sign-in` aborted, a hard `/my-work` navigation, and the sign-in page fully rendered in the trace). `signOut` now goes to `about:blank` before clearing cookies and asserts the landing URL; `identity-lifecycle.spec.ts:195` has passed on every CI run since. The single local `ERR_CONNECTION_REFUSED` was the separate libuv server crash (#79), not this race. **#79's crash is solved, and it is not this project's code (2026-09-22).** A ProcDump minidump gives the exception record as `0xC0000409` with fast-fail parameter **2**, `FAST_FAIL_STACK_COOKIE_CHECK_FAILURE` — a `/GS` stack-cookie failure, meaning genuine stack corruption, not the `FAST_FAIL_FATAL_APP_EXIT` that a deliberate Node or V8 abort raises. It matches open upstream bug libuv #5274, `uv__tcp_connect` on loopback connects, on every measured condition including the absent diagnostics. The defect is in libuv 1.51.0, which this machine's Node v24.15.0 vendors — and so does Node **v22.23.2**, the version `.nvmrc` and `engines.node` ask for, checked with `process.versions.uv`. **Moving to 22 is compliance, not a fix.** **CI was never susceptible:** both jobs are `runs-on: ubuntu-latest`, and the defect is in libuv's Windows backend, so it cannot occur there. An earlier version of this row attributed CI's lower failure rate to the runtime mismatch; that is withdrawn, and CI's own failures remain unexplained. The trigger here is the server's outbound loopback connects to `http://127.0.0.1:54321`. **Until those are off loopback, local browser results should not be read as evidence about this product.** **What the defect looked like while #79 was open, on both local and CI — resolved, see the head of this row:** `next start` exited `0xC0000409` locally after repeated `destination stream closed early` errors — four times on 2026-09-21 against once the previous day — and CI shows the same failure family at a lower rate. Every other local failure was a transport failure (`ERR_CONNECTION_REFUSED`, `ERR_NETWORK_IO_SUSPENDED`, or a navigation timeout inside `signIn` at `tests/e2e/auth.ts:110`), each matching a logged server exit or a network suspension. **Second environmental fault:** Windows suspended the browser's network stack mid-run three times, and one full run was discarded outright when the machine slept (Playwright reported a 1.7-day total). **Separate flaky test (#80), diagnosed and fixed on 2026-09-23:** `public-routes.spec.ts:27` failed once on CI in Firefox only — Chromium and WebKit passed it in the same run — then passed on re-run and 3 of 3 locally. **The cause recorded here earlier is withdrawn.** It was not the default 5s timeout against slow Firefox rendering. The failure was hidden behind a re-run, which is why it took a second look to find: `gh` serves only a run's latest attempt. Attempt 1 of run 35622498276 has it, and the retained trace settles it — the assertion failed with `element(s) not found`, and the trace records **no request to `/auth/v1/recover` at all**, not a slow one. The submit click landed 250ms after the page committed, before React had attached the form's handler, and was lost; Playwright's actionability checks passed because the button genuinely was present, visible and enabled. Measured afterwards on 2026-09-23: click-to-alert is 18–36ms in Firefox, and across 36 route, theme and width combinations every assertion in that file runs in 29–198ms against the 5s budget, so no timeout in it was ever a margin against slowness and raising one would have hidden a lost click. The hydration window is 229–394ms wide on this machine. Reproduced deliberately in Chromium by holding hydration open for four seconds: zero requests, no alert, and the browser falling back to a native form GET. `tests/e2e/interactive.ts` now waits for React to own a control before clicking it, used here and in the shared `signIn` and `completeMfa` helpers, which carry the identical shape. **A hypothesis, not a finding:** the unexplained `signIn` navigation timeouts recorded above have the same signature a lost sign-in click would produce, and that is worth watching rather than claiming. **A third signature appeared on 2026-09-21 after the above was written:** run 35631776763 on `ab50f8f` (a two-Markdown-file diff) failed `public-routes.spec.ts:232` on **WebKit only** with `page.goto: WebKit encountered an internal error` while navigating to `/crm`. That string appears in neither earlier failing run, so it is a distinct fault, and it does not touch `tests/e2e/auth.ts` — the `signOut` hypothesis above cannot explain it. Six CI runs over Markdown-only differences have now produced three failures in three different browsers. **That test now retries rather than reporting the fault:** `ae6d0ca`, merged in PR #81, catches this specific WebKit message and calls `page.goto` a second time. It suppresses the symptom in one test and does not address the cause, so it removes evidence from this row rather than adding any, and #79 stayed open at the time. **#79 was closed on 2026-09-23.** That retry still hides a WebKit fault rather than explaining it, and that remains true independently of #79. **2026-09-22, the crash reproduced twice under instrumentation** (once six minutes into a full suite, once after about ninety seconds). Both times it wrote **no Node diagnostic report, no Windows Application error event and no Windows Error Reporting entry**, although a control exception thrown under identical `NODE_OPTIONS` wrote a report immediately — the instrument works and the death bypasses it. The six-minute run was 19 passed and 22 `ERR_CONNECTION_REFUSED` after the server was gone. Six explanations are now eliminated by measurement: Node's zstd bindings (this server only returns gzip), aborted compression streams (6000 requests with 4554 cancelled mid-body, server survived), memory exhaustion (peak 467.6 MB and falling when it died), a dying Next render worker (`next start` is a single process, confirmed by the PID owning the port), application code aborting, and `npx` mangling the exit code. **`0xC0000409` is confirmed as the crash signature, by measurement.** It was withdrawn earlier on 2026-09-22 when two instrumented runs reported 127; that withdrawal was correct to make and wrong in its conclusion. 127 was Git Bash's rendering of an exit status it cannot express in a byte, not the process's own — first blamed on `npx`, which an `npx`-free run disproved. Launched from PowerShell with `Start-Process -PassThru`, the death at 14:26:53 reported -1073740791, which is `0xC0000409`, `STATUS_STACK_BUFFER_OVERRUN`. **That code also explains the three absences recorded above:** `0xC0000409` is raised by `__fastfail`, which terminates without unwinding, without running handlers and without the normal error-reporting path, so no Node report, no Application event and no WER entry is what it is supposed to leave. Node and V8 reach it through `IMMEDIATE_CRASH()`, the macro behind a failed `CHECK`. **The `MaxListenersExceededWarning` precursor claimed here earlier is withdrawn:** this run had zero of them. **And it is not one test:** three deaths are recorded at `identity-lifecycle.spec.ts:195`, `my-work.spec.ts:183` and `hello-hub.spec.ts:12`, at about six minutes, ninety seconds and eleven minutes; the last had completed 40 of 43 checks. A separate full 42-check run did complete 41 of 42 in 10.1 minutes with the server alive throughout, so the crash remains intermittent and a clean run still proves nothing. **2026-09-24, a flake resolved into a product defect and a clean full pass (#93).** `work-planning.spec.ts:44` had been failing about one run in eight, and it failed the Database security job on PR #92, a change that touches events and not tasks. The cause was in the task drawer, not in the test: every edit ends by re-reading the record, and that re-read blanked the drawer to a loading skeleton first, unmounting whatever the person had focused and dropping the next keystroke. A probe caught it directly — in ten pre-fix runs the drawer remounted 215–506ms before the keypress in the nine that passed and 85ms before it in the one that failed, where `document.activeElement` was null and the reorder never happened. **Two claims in #93 are withdrawn:** task volume is not the cause (7 of 8 at 55–63 tasks and 7 of 8 at 254–261 tasks — a five-fold increase that did not move the rate), and `revalidatePath("/", "layout")` was waste rather than the mechanism (a reorder settles in 483–549ms at 7 tasks and 577–777ms at 55, against a 30s poll). After the fix: `work-planning.spec.ts:44` 10 of 10 at 290–299 tasks — a higher volume than the pre-fix runs — and a full authenticated run of **50 of 50 in 6.8 minutes**, against 48 passed and 1 failed at the previous head. A new check, `an edit re-reads the drawer without throwing keyboard focus away`, fails against the unfixed drawer every time, replacing a one-in-eight race with a deterministic assertion. **And the suite now refuses to start against an unreachable Supabase.** Halfway through these measurements ten consecutive runs failed inside `signIn` because this machine's LAN address had changed and `NEXT_PUBLIC_SUPABASE_URL` is inlined at build time — the second time in two days, and the trap the execution journal had already described. `tests/e2e/supabase-preflight.ts` reads the address out of the served bundle's own JavaScript and stops the run in about five seconds, naming the dead address, instead of producing a screenful of failures that look like the product. It was proved against the stale build before the rebuild. **`main` itself was red when this was written, for two independent reasons, and one of them was #93.** Both of `main`'s last two runs — `c0ae048` (#94) and `0e8116a` (#95) — failed before this branch was merged into anything. `Database security` failed on `work-planning.spec.ts:44` with the defect above, now on `main` rather than only on a branch. `Verify` failed on `tests/unit/gmail-push-sync.test.ts`, which arrived with #94 and has an expiry date: it seeds an access token's expiry an hour after its fixture clock of `2026-09-24T05:00Z` while the code under test asks the real clock, correctly, so it passed on the run that merged it and has failed every run since that morning. Separated from this branch by running it on `origin/main` in a worktree, where it fails identically, and fixed here because nothing on this branch could be verified through CI until it was. Merged-head evidence: **50 of 50 authenticated browser checks in 6.5 minutes**, 515 unit tests, 456 database assertions across 19 files. Hosted staging evidence remains outstanding |
| Full acceptance | See acceptance-matrix.md; no overall acceptance certification |
| Operations | Document quarantine is enforced in code/RLS, but a QBBE-controlled ClamAV host, restore/backup/alert custody, and operator sign-off are not established |

## External inputs

- QBBE provider account-owner email, existing sender domain and secure provider access.
- Actual VMS contract, QBBE Google configuration and authorized test recipients.
- Named product/privacy owner, operator, recovery custodian and alert recipients.
- Isolated restore environment and named Safari/mobile acceptance operator.

Do not put credentials, backup data or recovery keys in this report. Continue
independent work while inputs are missing. A disabled integration is not full
readiness, even if a separate explicitly labelled pilot could proceed.

## Final gate

Require every mandatory acceptance row verified at the frozen release commit;
complete lint/types/unit/build/dependency/database/browser/concurrency checks;
document p95 interaction ≤2 seconds and realtime ≤5 seconds on the agreed
50-user fixture; prove encrypted database/file restore within 24-hour RPO and
one-business-day RTO; obtain named operator sign-off. Attach staging URL,
environment inventory, administrator guide, recovery guide and production
release procedure. Missing evidence leaves readiness blocked.
